# ═══════════════════════════════════════════════════════════════════
# Gobanion – Development Environment
# ═══════════════════════════════════════════════════════════════════
# Committed to git. 本地开发环境专用值，覆盖 .env.shared。

APP_ENV=development
DEBUG=true

# 开发环境 LLM（本地模型，上下文中等）
LLM_API_BASE=http://localhost:8000/v1
LLM_MODEL=Qwen3.5-9B
LLM_MAX_TOKENS=8192
LLM_TEMPERATURE=0.7
